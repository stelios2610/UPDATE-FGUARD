# FGUARD Debian 12 Preseed
# Automated installer configuration

# Locale
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network — let installer use DHCP for install, first-boot handles config
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string fguard
d-i netcfg/get_domain string aegis.local
d-i netcfg/wireless_wep string

# Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Clock
d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

# Disk partitioning — use entire first disk, no LVM
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Root password (change after install!)
d-i passwd/root-login boolean true
d-i passwd/root-password password FGUARD2024!
d-i passwd/root-password-again password FGUARD2024!

# No normal user
d-i passwd/make-user boolean false

# Base packages
d-i tasksel/first multiselect standard
d-i pkgsel/include string openssh-server python3 python3-pip curl rsync
d-i pkgsel/upgrade select full-upgrade

# Boot loader
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean false
d-i grub-installer/bootdev string default

# No popularity contest
popularity-contest popularity-contest/participate boolean false

# Post-install: run FGUARD installer
d-i preseed/late_command string \
    in-target mkdir -p /opt/fguard ; \
    in-target cp -r /cdrom/fguard/. /opt/fguard/ ; \
    in-target bash /opt/fguard/build/install.sh ; \
    in-target systemctl enable fguard-firstboot fguard

# Reboot after install
d-i finish-install/reboot_in_progress note
